# 延迟求和波束形成 (Delay-and-Sum Beamforming)

## 1. 基本原理

**延迟求和 (Delay-and-Sum, DS)** 是最简单、最经典的波束形成方法。其核心思想是：通过对各麦克风信号施加适当的时延，使目标方向的信号在相位上对齐，然后求和以增强目标信号。

### 1.1 物理直观

```
声源 (θ₀)
    ↓
   🔊
  / | \
 /  |  \
🎤 🎤 🎤  ← 信号到达时间不同
 ↓  ↓  ↓
[τ₁ τ₂ τ₃] ← 补偿时延
 ↓  ↓  ↓
 同相叠加 → 增强
```

**步骤**：
1. **延迟 (Delay)**：补偿各麦克风的传播时延差
2. **求和 (Sum)**：将对齐后的信号相加

---

## 2. 数学推导

### 2.1 时域模型

第 $p$ 个麦克风接收到的信号：
$$x_p(t) = s(t - \tau_p) + n_p(t)$$

其中 $\tau_p$ 是声波从源到第 $p$ 个麦克风的传播时延。

**延迟补偿**：
$$x_p(t + \tau_p) = s(t) + n_p(t + \tau_p)$$

**求和**：
$$y(t) = \frac{1}{P}\sum_{p=1}^{P} x_p(t + \tau_p)$$

$$y(t) = s(t) + \frac{1}{P}\sum_{p=1}^{P} n_p(t + \tau_p)$$

**信号增强**：
- 信号部分：$P$ 个同相信号相加，幅度增加 $P$ 倍
- 噪声部分：假设不相关，功率增加 $P$ 倍，幅度增加 $\sqrt{P}$ 倍

**SNR改善**：
$$\text{SNR}_{\text{out}} = P \cdot \text{SNR}_{\text{in}}$$

$$\text{SNR}_{\text{gain}} = 10\log_{10}(P) \text{ dB}$$

### 2.2 频域模型

通过STFT变换到频域：

$$X_p(f,t) = S(f,t) e^{-j2\pi f \tau_p} + N_p(f,t)$$

**相位补偿**：
$$X_p(f,t) e^{j2\pi f \tau_p} = S(f,t) + N_p(f,t) e^{j2\pi f \tau_p}$$

**求和**：
$$Y(f,t) = \frac{1}{P}\sum_{p=1}^{P} X_p(f,t) e^{j2\pi f \tau_p}$$

### 2.3 向量形式

定义导向矢量：
$$\mathbf{d}(\theta_0, f) = [e^{-j2\pi f \tau_1}, e^{-j2\pi f \tau_2}, \ldots, e^{-j2\pi f \tau_P}]^T$$

**DS权重**：
$$\mathbf{w}_{\text{DS}}(f) = \frac{1}{P}\mathbf{d}^*(\theta_0, f)$$

**输出**：
$$Y(f,t) = \mathbf{w}_{\text{DS}}^H(f) \mathbf{X}(f,t)$$

---

## 3. 远场近似

### 3.1 平面波假设

当声源距离 $r \gg D$（阵列孔径）时，可以假设平面波到达。

**时延计算**（线性阵列）：

以第一个麦克风为参考：
$$\tau_p = \frac{(p-1)d \sin\theta_0}{c}$$

其中：
- $d$：麦克风间距
- $\theta_0$：目标方向（相对于阵列法线）
- $c$：声速

**导向矢量**：
$$d_p(\theta_0, f) = e^{-j2\pi f (p-1)d \sin\theta_0 / c} = e^{-j(p-1)kd\sin\theta_0}$$

其中 $k = 2\pi f / c$ 是波数。

### 3.2 近场模型

对于近场源，需要考虑球面波：

$$\tau_p = \frac{\|\mathbf{r}_s - \mathbf{r}_p\| - \|\mathbf{r}_s - \mathbf{r}_{\text{ref}}\|}{c}$$

其中：
- $\mathbf{r}_s$：声源位置
- $\mathbf{r}_p$：第 $p$ 个麦克风位置
- $\mathbf{r}_{\text{ref}}$：参考麦克风位置

---

## 4. 波束图分析

### 4.1 波束图推导

对于均匀线性阵列，目标方向 $\theta_0 = 0$：

$$\mathbf{w} = \frac{1}{P}[1, 1, \ldots, 1]^T$$

波束图：
$$B(\theta) = \mathbf{w}^H \mathbf{d}(\theta) = \frac{1}{P}\sum_{p=1}^{P} e^{-j(p-1)kd\sin\theta}$$

这是一个几何级数：
$$B(\theta) = \frac{1}{P} \cdot \frac{1 - e^{-jPkd\sin\theta}}{1 - e^{-jkd\sin\theta}}$$

**幅度**：
$$|B(\theta)| = \frac{1}{P}\left|\frac{\sin(Pkd\sin\theta/2)}{\sin(kd\sin\theta/2)}\right|$$

### 4.2 主瓣宽度

**第一零点位置**：

令分子为零：
$$Pkd\sin\theta/2 = \pi$$

$$\sin\theta = \frac{\lambda}{Pd} = \frac{\lambda}{D}$$

对于小角度（$\theta \ll 1$）：
$$\theta_{\text{null}} \approx \frac{\lambda}{D}$$

**3dB带宽**：

$$\Delta\theta_{3dB} \approx 0.886 \frac{\lambda}{D}$$

**观察**：
- 主瓣宽度与阵列孔径 $D$ 成反比
- 主瓣宽度与波长 $\lambda$（频率的倒数）成正比
- 频率越高，主瓣越窄，指向性越强

### 4.3 旁瓣特性

**第一旁瓣位置**：

$$Pkd\sin\theta/2 = 3\pi/2$$

**第一旁瓣电平**：

$$SLL_1 = 20\log_{10}\left(\frac{2}{3\pi}\right) \approx -13.5 \text{ dB}$$

这是DS波束形成的固有特性，与阵列参数无关。

---

## 5. 性能分析

### 5.1 白噪声增益

假设噪声在各麦克风不相关：

$$\mathbf{R}_N = \sigma_n^2 \mathbf{I}$$

白噪声增益：
$$WNG = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\mathbf{w}^H\mathbf{w}}$$

对于DS：
$$\mathbf{w} = \frac{1}{P}\mathbf{d}^*(\theta_0)$$

$$WNG = \frac{|P/P|^2}{(1/P^2) \cdot P} = P$$

$$WNG_{dB} = 10\log_{10}(P)$$

**结论**：$P$ 个麦克风的DS波束形成有 $10\log_{10}(P)$ dB的白噪声增益。

**示例**：
- 2麦克风：3 dB
- 4麦克风：6 dB
- 8麦克风：9 dB

### 5.2 指向性因子

对于线性阵列：

$$DF = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\frac{1}{2\pi}\int_{-\pi}^{\pi} |\mathbf{w}^H\mathbf{d}(\theta)|^2 d\theta}$$

**近似公式**（$kD \gg 1$）：

$$DF \approx \frac{2D}{\lambda} = \frac{2Df}{c}$$

$$DI = 10\log_{10}(DF) \approx 10\log_{10}\left(\frac{2Df}{c}\right)$$

**观察**：
- 指向性因子与频率成正比
- 低频时指向性差
- 高频时指向性强

### 5.3 阵列增益

对于空间白噪声：
$$AG = WNG = P$$

对于扩散噪声场：
$$AG = DF \approx \frac{2D}{\lambda}$$

**频率依赖性**：
- 低频：$DF < P$，扩散噪声抑制差
- 高频：$DF > P$，扩散噪声抑制好

---

## 6. 实现方法

### 6.1 时域实现

**步骤**：
1. 计算各麦克风的时延 $\tau_p$
2. 对信号进行延迟（插值）
3. 求和并归一化

```python
def delay_and_sum_time(signals, delays, fs):
    """
    时域延迟求和波束形成

    参数:
        signals: [P, T] - P个麦克风，T个采样点
        delays: [P] - 各麦克风的延迟（秒）
        fs: 采样率
    """
    P, T = signals.shape
    output = np.zeros(T)

    for p in range(P):
        # 计算延迟的采样点数
        delay_samples = delays[p] * fs

        # 分数延迟插值
        delayed = fractional_delay(signals[p], delay_samples)
        output += delayed

    return output / P

def fractional_delay(signal, delay):
    """分数延迟实现（使用sinc插值）"""
    # 简化实现：使用scipy.signal.resample或FFT相移
    pass
```

### 6.2 频域实现

**步骤**：
1. STFT变换到频域
2. 对每个频点应用相位补偿
3. 求和
4. iSTFT变换回时域

```python
def delay_and_sum_freq(X, d_vec, theta0, f, c=343):
    """
    频域延迟求和波束形成

    参数:
        X: [P, F, T] - 频域信号
        d_vec: [P] - 麦克风位置（相对于参考点）
        theta0: 目标方向（弧度）
        f: [F] - 频率数组
        c: 声速
    """
    P, F, T = X.shape
    Y = np.zeros((F, T), dtype=complex)

    for fi in range(F):
        # 计算导向矢量
        k = 2 * np.pi * f[fi] / c
        steering = np.exp(-1j * k * d_vec * np.sin(theta0))

        # 延迟求和
        w = steering.conj() / P
        Y[fi, :] = w @ X[:, fi, :]

    return Y
```

### 6.3 子带实现

在子带域进行处理，平衡计算复杂度和延迟。

---

## 7. 优缺点分析

### 7.1 优点

1. **简单直观**：原理清晰，易于理解和实现
2. **鲁棒性好**：对阵列误差和环境变化不敏感
3. **无需统计信息**：不需要噪声协方差矩阵
4. **计算量小**：只需简单的延迟和求和
5. **白噪声增益高**：$WNG = P$，不会放大传感器噪声

### 7.2 缺点

1. **非自适应**：无法根据噪声环境调整
2. **旁瓣固定**：第一旁瓣约-13.5 dB，无法降低
3. **低频性能差**：指向性随频率降低而下降
4. **需要DOA**：必须知道目标方向
5. **干扰抑制弱**：对相干干扰抑制能力有限

---

## 8. 改进方法

### 8.1 加权延迟求和

引入幅度加权以降低旁瓣：

$$\mathbf{w} = \frac{\mathbf{a} \odot \mathbf{d}^*(\theta_0)}{\mathbf{a}^T\mathbf{d}^*(\theta_0)}$$

其中 $\mathbf{a}$ 是幅度权重向量，$\odot$ 是逐元素乘法。

**常用窗函数**：
- Hamming窗
- Hanning窗
- Chebyshev窗

**效果**：
- 降低旁瓣电平
- 主瓣变宽
- 白噪声增益降低

### 8.2 频率依赖权重

对不同频率使用不同的权重，实现频率不变波束形成。

### 8.3 近场聚焦

对于近场源，使用球面波模型计算时延。

---

## 9. 应用场景

### 9.1 会议系统

**配置**：
- 线性阵列，4-8个麦克风
- 间距：3-5 cm
- 指向会议桌

**优势**：
- 简单可靠
- 低延迟
- 成本低

### 9.2 助听器

**配置**：
- 2个麦克风
- 间距：1-2 cm
- 前向指向

**优势**：
- 计算量极小
- 功耗低
- 3 dB增益

### 9.3 麦克风阵列模块

**配置**：
- 圆形或线性阵列
- 6-8个麦克风
- 可切换方向

**优势**：
- 灵活性
- 鲁棒性
- 易于集成

---

## 10. 数值示例

### 10.1 4元线性阵列

**参数**：
- $P = 4$
- $d = 0.05$ m
- $f = 2000$ Hz
- $\theta_0 = 0°$

**计算**：

波数：$k = 2\pi f / c = 36.6$ rad/m

主瓣宽度：
$$\theta_{\text{null}} = \arcsin\left(\frac{c}{fPd}\right) = \arcsin\left(\frac{343}{2000 \times 4 \times 0.05}\right) \approx 31°$$

白噪声增益：
$$WNG = 10\log_{10}(4) = 6 \text{ dB}$$

指向性因子：
$$DF \approx \frac{2 \times 0.15 \times 2000}{343} \approx 1.75$$
$$DI \approx 2.4 \text{ dB}$$

### 10.2 完整Python实现

```python
import numpy as np
import matplotlib.pyplot as plt
from scipy.signal import stft, istft

class DelayAndSumBeamformer:
    def __init__(self, mic_positions, fs, c=343):
        """
        参数:
            mic_positions: [P, 3] - 麦克风位置坐标
            fs: 采样率
            c: 声速
        """
        self.mic_pos = np.array(mic_positions)
        self.P = len(mic_positions)
        self.fs = fs
        self.c = c

    def steer(self, theta, phi=0):
        """
        计算导向矢量

        参数:
            theta: 方位角（弧度）
            phi: 俯仰角（弧度）
        """
        # 方向单位向量
        u = np.array([
            np.cos(phi) * np.sin(theta),
            np.cos(phi) * np.cos(theta),
            np.sin(phi)
        ])

        # 计算时延
        ref_pos = self.mic_pos[0]
        delays = -np.dot(self.mic_pos - ref_pos, u) / self.c

        return delays

    def process_freq(self, X, theta, phi=0):
        """
        频域处理

        参数:
            X: [P, F, T] - 频域信号
            theta, phi: 目标方向
        """
        P, F, T = X.shape

        # 计算时延
        delays = self.steer(theta, phi)

        # 频率数组
        freqs = np.fft.rfftfreq(2*(F-1), 1/self.fs)

        # 应用延迟求和
        Y = np.zeros((F, T), dtype=complex)
        for fi, f in enumerate(freqs):
            # 相位补偿
            phase_shift = np.exp(1j * 2 * np.pi * f * delays)
            w = phase_shift.conj() / P
            Y[fi, :] = w @ X[:, fi, :]

        return Y

    def process(self, signals, theta, phi=0):
        """
        完整处理流程

        参数:
            signals: [P, T] - 时域信号
            theta, phi: 目标方向
        """
        # STFT
        f, t, X = stft(signals, fs=self.fs, nperseg=512)

        # 延迟求和
        Y = self.process_freq(X, theta, phi)

        # iSTFT
        _, y = istft(Y, fs=self.fs, nperseg=512)

        return y

# 使用示例
mic_pos = np.array([[0, 0, 0], [0.05, 0, 0],
                    [0.10, 0, 0], [0.15, 0, 0]])
bf = DelayAndSumBeamformer(mic_pos, fs=16000)

# 处理信号
# signals = ... # [4, T]
# output = bf.process(signals, theta=0)
```

