# 波束形成数学基础

## 1. 阵列信号模型

### 1.1 远场平面波假设

当声源距离阵列足够远时（通常 $r > 2D^2/\lambda$，其中 $D$ 是阵列孔径），可以假设到达阵列的声波为**平面波**。

```
声源 (远场)
    ↓
    ↓ 平面波前
────────────
🎤  🎤  🎤  🎤
```

### 1.2 时域信号模型

考虑 $P$ 个麦克风的线性阵列，声源位于方向 $\theta$，第 $p$ 个麦克风接收到的信号：

$$x_p(t) = s(t - \tau_p(\theta)) + n_p(t)$$

其中：
- $s(t)$：源信号
- $\tau_p(\theta)$：声波到达第 $p$ 个麦克风相对于参考点的时延
- $n_p(t)$：噪声

### 1.3 频域信号模型

通过STFT变换到频域：

$$X_p(f,t) = S(f,t) e^{-j2\pi f \tau_p(\theta)} + N_p(f,t)$$

**向量形式**：

$$\mathbf{X}(f,t) = \mathbf{d}(\theta,f) S(f,t) + \mathbf{N}(f,t)$$

其中 $\mathbf{d}(\theta,f)$ 是**导向矢量**。

---

## 2. 导向矢量 (Steering Vector)

### 2.1 定义

导向矢量描述平面波从方向 $\theta$ 到达阵列时的相位关系：

$$\mathbf{d}(\theta,f) = [e^{-j2\pi f \tau_1(\theta)}, e^{-j2\pi f \tau_2(\theta)}, \ldots, e^{-j2\pi f \tau_P(\theta)}]^T$$

### 2.2 线性阵列的导向矢量

对于间距为 $d$ 的均匀线性阵列（ULA），以第一个麦克风为参考：

$$\tau_p(\theta) = \frac{(p-1)d \sin\theta}{c}$$

因此：

$$d_p(\theta,f) = e^{-j2\pi f (p-1)d \sin\theta / c} = e^{-j(p-1)\omega d \sin\theta / c}$$

其中 $\omega = 2\pi f$，$c$ 是声速（约343 m/s）。

**归一化形式**：

$$d_p(\theta,f) = e^{-j(p-1)kd\sin\theta}$$

其中 $k = 2\pi f / c$ 是波数。

### 2.3 圆形阵列的导向矢量

对于半径为 $r$ 的圆形阵列，第 $p$ 个麦克风位于角度 $\phi_p = 2\pi(p-1)/P$：

$$\tau_p(\theta) = \frac{r \cos(\theta - \phi_p)}{c}$$

$$d_p(\theta,f) = e^{-j2\pi f r \cos(\theta - \phi_p) / c}$$

### 2.4 任意阵列的导向矢量

对于任意位置的麦克风阵列，设第 $p$ 个麦克风位置为 $\mathbf{r}_p = [x_p, y_p, z_p]^T$，声源方向单位矢量为 $\mathbf{u}(\theta, \phi)$：

$$\tau_p(\theta, \phi) = -\frac{\mathbf{u}^T \mathbf{r}_p}{c}$$

$$d_p(\theta, \phi, f) = e^{j2\pi f \mathbf{u}^T \mathbf{r}_p / c}$$

---

## 3. 波束形成器输出

### 3.1 基本公式

波束形成器对麦克风信号进行加权求和：

$$Y(f,t) = \sum_{p=1}^{P} w_p^*(f) X_p(f,t) = \mathbf{w}^H(f) \mathbf{X}(f,t)$$

其中：
- $\mathbf{w}(f) = [w_1(f), w_2(f), \ldots, w_P(f)]^T$：权重向量
- $(\cdot)^H$：共轭转置

### 3.2 输出信号分解

将输入信号分解为目标信号和噪声：

$$Y(f,t) = \mathbf{w}^H \mathbf{d}(\theta_0,f) S(f,t) + \mathbf{w}^H \mathbf{N}(f,t)$$

$$Y(f,t) = Y_s(f,t) + Y_n(f,t)$$

### 3.3 输出功率

$$P_Y = E[|Y(f,t)|^2] = \mathbf{w}^H \mathbf{R}_X \mathbf{w}$$

其中 $\mathbf{R}_X = E[\mathbf{X}\mathbf{X}^H]$ 是输入信号的协方差矩阵。

---

## 4. 阵列响应与波束图

### 4.1 阵列响应函数

阵列对来自方向 $\theta$ 的单位幅度平面波的响应：

$$B(\theta,f) = \mathbf{w}^H(f) \mathbf{d}(\theta,f)$$

这是一个复数，表示幅度和相位响应。

### 4.2 波束图 (Beam Pattern)

波束图是阵列响应的幅度：

$$|B(\theta,f)| = |\mathbf{w}^H(f) \mathbf{d}(\theta,f)|$$

通常以dB表示：

$$B_{dB}(\theta,f) = 20\log_{10}|B(\theta,f)|$$

### 4.3 归一化波束图

归一化到目标方向的响应为1：

$$B_{\text{norm}}(\theta,f) = \frac{\mathbf{w}^H(f) \mathbf{d}(\theta,f)}{\mathbf{w}^H(f) \mathbf{d}(\theta_0,f)}$$

### 4.4 波束图特征

**主瓣 (Main Lobe)**：
- 中心：目标方向 $\theta_0$
- 宽度：3dB带宽（半功率点）

**旁瓣 (Side Lobes)**：
- 位置：非目标方向的峰值
- 电平：相对主瓣的dB值

**零点 (Nulls)**：
- 响应为零的方向

---

## 5. 协方差矩阵

### 5.1 定义

输入信号的协方差矩阵：

$$\mathbf{R}_X(f) = E[\mathbf{X}(f,t)\mathbf{X}^H(f,t)]$$

### 5.2 信号加噪声模型

假设信号和噪声不相关：

$$\mathbf{R}_X = \mathbf{R}_S + \mathbf{R}_N$$

其中：
$$\mathbf{R}_S = \sigma_s^2 \mathbf{d}(\theta_0) \mathbf{d}^H(\theta_0)$$
$$\mathbf{R}_N = E[\mathbf{N}\mathbf{N}^H]$$

### 5.3 噪声协方差矩阵

**空间白噪声**（各向同性）：
$$\mathbf{R}_N = \sigma_n^2 \mathbf{I}$$

**相干噪声**：
$$\mathbf{R}_N = \sigma_n^2 \mathbf{d}(\theta_n) \mathbf{d}^H(\theta_n)$$

**扩散噪声场**：
$$[\mathbf{R}_N]_{ij} = \sigma_n^2 \text{sinc}(kd_{ij})$$

其中 $d_{ij}$ 是麦克风 $i,j$ 间距，$\text{sinc}(x) = \sin(x)/x$。

### 5.4 实际估计

在实践中，协方差矩阵通过时间平均估计：

$$\hat{\mathbf{R}}_X(f) = \frac{1}{T}\sum_{t=1}^{T} \mathbf{X}(f,t)\mathbf{X}^H(f,t)$$

---

## 6. 性能指标

### 6.1 阵列增益 (Array Gain)

定义为输出SNR相对于单个麦克风SNR的改善：

$$AG = \frac{\text{SNR}_{\text{out}}}{\text{SNR}_{\text{in}}} = \frac{|\mathbf{w}^H\mathbf{d}|^2 / \mathbf{w}^H\mathbf{R}_N\mathbf{w}}{\sigma_s^2 / \sigma_n^2}$$

简化为：

$$AG = \frac{|\mathbf{w}^H\mathbf{d}|^2}{\mathbf{w}^H\mathbf{R}_N\mathbf{w}} \cdot \frac{\sigma_n^2}{\sigma_s^2} \cdot \frac{\sigma_s^2}{\sigma_n^2} = \frac{|\mathbf{w}^H\mathbf{d}|^2}{\mathbf{w}^H\mathbf{R}_N\mathbf{w}}$$

### 6.2 白噪声增益 (White Noise Gain)

假设噪声为空间白噪声（$\mathbf{R}_N = \sigma_n^2\mathbf{I}$）：

$$WNG = \frac{|\mathbf{w}^H\mathbf{d}|^2}{\mathbf{w}^H\mathbf{w}}$$

dB形式：

$$WNG_{dB} = 10\log_{10}\frac{|\mathbf{w}^H\mathbf{d}|^2}{\|\mathbf{w}\|^2}$$

### 6.3 指向性因子 (Directivity Factor)

衡量阵列集中能量到目标方向的能力：

$$DF = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\int_{-\pi}^{\pi} |\mathbf{w}^H\mathbf{d}(\theta)|^2 d\theta}$$

**指向性指数**：

$$DI = 10\log_{10}(DF)$$

### 6.4 前后比 (Front-to-Back Ratio)

主瓣方向与反方向的响应比：

$$FBR = \frac{|B(\theta_0)|^2}{|B(\theta_0 + \pi)|^2}$$

---

## 7. 约束条件

### 7.1 无失真约束

保证目标方向的信号无失真通过：

$$\mathbf{w}^H \mathbf{d}(\theta_0) = 1$$

或更一般的：

$$\mathbf{w}^H \mathbf{d}(\theta_0) = g$$

其中 $g$ 是期望增益。

### 7.2 零点约束

在干扰方向 $\theta_i$ 形成零点：

$$\mathbf{w}^H \mathbf{d}(\theta_i) = 0$$

### 7.3 多约束形式

矩阵形式的线性约束：

$$\mathbf{C}^H \mathbf{w} = \mathbf{f}$$

其中：
- $\mathbf{C} = [\mathbf{d}(\theta_0), \mathbf{d}(\theta_1), \ldots]$：约束矩阵
- $\mathbf{f} = [1, 0, \ldots]^T$：约束向量

---

## 8. 优化问题

### 8.1 最小方差准则

在无失真约束下，最小化输出功率：

$$\min_{\mathbf{w}} \mathbf{w}^H \mathbf{R}_X \mathbf{w} \quad \text{s.t.} \quad \mathbf{w}^H \mathbf{d}(\theta_0) = 1$$

### 8.2 最大SNR准则

最大化输出信噪比：

$$\max_{\mathbf{w}} \frac{|\mathbf{w}^H \mathbf{d}|^2}{\mathbf{w}^H \mathbf{R}_N \mathbf{w}}$$

### 8.3 最大指向性准则

最大化指向性因子：

$$\max_{\mathbf{w}} \frac{|\mathbf{w}^H \mathbf{d}(\theta_0)|^2}{\mathbf{w}^H \mathbf{\Gamma} \mathbf{w}}$$

其中 $\mathbf{\Gamma}$ 是扩散噪声场的协方差矩阵。

---

## 9. 数值示例

### 9.1 4元线性阵列

**配置**：
- 麦克风数量：$P = 4$
- 间距：$d = 0.05$ m
- 频率：$f = 1000$ Hz
- 目标方向：$\theta_0 = 0°$（正前方）

**导向矢量计算**：

$$k = \frac{2\pi f}{c} = \frac{2\pi \times 1000}{343} \approx 18.3 \text{ rad/m}$$

$$kd = 18.3 \times 0.05 \approx 0.915 \text{ rad}$$

对于 $\theta = 0°$：

$$\mathbf{d}(0°) = [1, 1, 1, 1]^T$$

对于 $\theta = 30°$：

$$\mathbf{d}(30°) = [1, e^{-j0.458}, e^{-j0.915}, e^{-j1.373}]^T$$

### 9.2 延迟求和波束形成

权重：$\mathbf{w} = \frac{1}{P}\mathbf{d}^*(\theta_0) = \frac{1}{4}[1, 1, 1, 1]^T$

**波束图**：

$$B(\theta) = \frac{1}{4}\sum_{p=1}^{4} e^{-j(p-1)kd\sin\theta}$$

$$|B(\theta)| = \frac{1}{4}\left|\frac{\sin(2kd\sin\theta)}{\sin(0.5kd\sin\theta)}\right|$$

**主瓣宽度**（3dB）：约 $\pm 30°$
