# 阵列几何与性能分析

## 1. 麦克风阵列几何

### 1.1 线性阵列 (Linear Array)

**均匀线性阵列 (ULA - Uniform Linear Array)**：

麦克风等间距排列在一条直线上。

```
🎤────d────🎤────d────🎤────d────🎤
p=1       p=2       p=3       p=4
```

**位置坐标**：
$$\mathbf{r}_p = [(p-1)d, 0, 0]^T, \quad p = 1, 2, \ldots, P$$

**优点**：
- 结构简单
- 易于分析
- 成本低

**缺点**：
- 只能估计一维角度
- 存在前后模糊

**应用**：会议系统、条形音箱

---

### 1.2 圆形阵列 (Circular Array)

**均匀圆形阵列 (UCA - Uniform Circular Array)**：

麦克风均匀分布在圆周上。

```
        🎤 (p=1)
    🎤         🎤
  🎤      ⊙      🎤
    🎤         🎤
        🎤 (p=P)
```

**位置坐标**：
$$\mathbf{r}_p = [r\cos\phi_p, r\sin\phi_p, 0]^T$$

其中 $\phi_p = 2\pi(p-1)/P$ 是第 $p$ 个麦克风的角度。

**优点**：
- 360°全向覆盖
- 无前后模糊
- 对称性好

**缺点**：
- 制造成本较高
- 分析较复杂

**应用**：智能音箱、机器人

---

### 1.3 平面阵列 (Planar Array)

**矩形阵列**：

麦克风排列在平面网格上。

```
🎤  🎤  🎤  🎤
🎤  🎤  🎤  🎤
🎤  🎤  🎤  🎤
```

**位置坐标**：
$$\mathbf{r}_{mn} = [md_x, nd_y, 0]^T$$

**优点**：
- 可估计二维角度（方位角和俯仰角）
- 高分辨率

**缺点**：
- 麦克风数量多
- 计算复杂度高

**应用**：雷达、声学成像

---

### 1.4 三维阵列 (3D Array)

麦克风分布在三维空间中。

**优点**：
- 完整的3D定位
- 最高的空间分辨率

**缺点**：
- 结构复杂
- 成本高

**应用**：专业声学测量

---

## 2. 阵列参数

### 2.1 阵列孔径 (Array Aperture)

阵列的物理尺寸，通常指最远两个麦克风的距离。

$$D = \max_{i,j} \|\mathbf{r}_i - \mathbf{r}_j\|$$

**影响**：
- 孔径越大，空间分辨率越高
- 孔径越大，低频性能越好

### 2.2 麦克风间距 (Inter-element Spacing)

相邻麦克风之间的距离 $d$。

**空间采样定理**：

为避免空间混叠，间距应满足：
$$d \leq \frac{\lambda}{2} = \frac{c}{2f}$$

其中 $\lambda$ 是波长，$c$ 是声速，$f$ 是频率。

**示例**：
- $f = 8$ kHz：$d \leq 2.14$ cm
- $f = 4$ kHz：$d \leq 4.29$ cm

**权衡**：
- 间距小：避免混叠，但阵列孔径受限
- 间距大：增大孔径，但高频会混叠

### 2.3 麦克风数量

**影响**：
- 数量越多，阵列增益越大
- 数量越多，自由度越高
- 数量越多，成本和计算量增加

**典型配置**：
- 2麦克风：简单立体声
- 4-6麦克风：智能音箱
- 8-16麦克风：专业会议系统
- 32+麦克风：声学成像

---

## 3. 波束图分析

### 3.1 波束图定义

波束图描述阵列对不同方向信号的响应：

$$B(\theta, f) = \mathbf{w}^H(f) \mathbf{d}(\theta, f)$$

**归一化波束图**：
$$B_{\text{norm}}(\theta) = \frac{|\mathbf{w}^H \mathbf{d}(\theta)|}{|\mathbf{w}^H \mathbf{d}(\theta_0)|}$$

**dB形式**：
$$B_{dB}(\theta) = 20\log_{10}|B_{\text{norm}}(\theta)|$$

### 3.2 波束图特征

**主瓣 (Main Lobe)**：

目标方向附近的高响应区域。

- **主瓣宽度**：3dB带宽（半功率点）
  $$\Delta\theta_{3dB} \approx \frac{0.886\lambda}{D}$$
  
- **主瓣方向**：$\theta_0$（目标方向）

**旁瓣 (Side Lobes)**：

非目标方向的响应峰值。

- **旁瓣电平 (SLL)**：最大旁瓣相对主瓣的dB值
  $$SLL = 20\log_{10}\frac{\max_{\theta \neq \theta_0}|B(\theta)|}{|B(\theta_0)|}$$

**零点 (Nulls)**：

响应为零的方向，$B(\theta_{\text{null}}) = 0$。

### 3.3 线性阵列波束图推导

对于 $P$ 元均匀线性阵列，延迟求和波束形成：

$$\mathbf{w} = \frac{1}{P}\mathbf{d}^*(\theta_0)$$

波束图：
$$B(\theta) = \frac{1}{P}\sum_{p=1}^{P} e^{-j(p-1)kd(\sin\theta - \sin\theta_0)}$$

令 $\psi = kd(\sin\theta - \sin\theta_0)$，则：

$$B(\theta) = \frac{1}{P}\sum_{p=1}^{P} e^{-j(p-1)\psi} = \frac{1}{P} \cdot \frac{1 - e^{-jP\psi}}{1 - e^{-j\psi}}$$

$$|B(\theta)| = \frac{1}{P}\left|\frac{\sin(P\psi/2)}{\sin(\psi/2)}\right|$$

**主瓣宽度**（第一零点）：

$$P\psi/2 = \pi \Rightarrow kd(\sin\theta - \sin\theta_0) = \frac{2\pi}{P}$$

对于 $\theta_0 = 0$：
$$\sin\theta \approx \theta \approx \frac{\lambda}{Pd} = \frac{\lambda}{D}$$

因此主瓣宽度约为：
$$\Delta\theta \approx \frac{2\lambda}{D}$$

---

## 4. 性能指标详解

### 4.1 指向性因子 (Directivity Factor)

定义为主瓣方向的功率与全空间平均功率的比值：

$$DF = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\frac{1}{4\pi}\int_{\Omega} |\mathbf{w}^H\mathbf{d}(\theta,\phi)|^2 d\Omega}$$

对于2D情况（只考虑方位角）：

$$DF = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\frac{1}{2\pi}\int_{-\pi}^{\pi} |\mathbf{w}^H\mathbf{d}(\theta)|^2 d\theta}$$

**指向性指数 (DI)**：
$$DI = 10\log_{10}(DF) \quad [\text{dB}]$$

**物理意义**：
- DI越大，阵列越能集中能量到目标方向
- DI越大，对扩散噪声的抑制能力越强

**典型值**：
- 单麦克风：DI = 0 dB
- 2元阵列：DI ≈ 3 dB
- 4元阵列：DI ≈ 6 dB
- 8元阵列：DI ≈ 9 dB

### 4.2 白噪声增益 (White Noise Gain)

假设噪声为空间白噪声（各向同性、不相关）：

$$WNG = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\mathbf{w}^H\mathbf{w}} = \frac{|\mathbf{w}^H\mathbf{d}(\theta_0)|^2}{\|\mathbf{w}\|^2}$$

**dB形式**：
$$WNG_{dB} = 10\log_{10}(WNG)$$

**物理意义**：
- 衡量对不相关噪声的抑制能力
- WNG低表示权重向量范数大，可能放大传感器噪声

**与阵列增益的关系**：

对于空间白噪声：
$$AG = WNG$$

对于相干噪声：
$$AG \neq WNG$$

**延迟求和的WNG**：

$$\mathbf{w} = \frac{1}{P}\mathbf{d}^*(\theta_0)$$

$$WNG = \frac{|P/P|^2}{P \cdot 1/P^2} = P$$

$$WNG_{dB} = 10\log_{10}(P)$$

即 $P$ 个麦克风的延迟求和有 $10\log_{10}(P)$ dB的白噪声增益。

### 4.3 阵列增益 (Array Gain)

定义为输出SNR相对于单麦克风SNR的改善：

$$AG = \frac{\text{SNR}_{\text{out}}}{\text{SNR}_{\text{in}}}$$

展开：
$$AG = \frac{|\mathbf{w}^H\mathbf{d}|^2 \sigma_s^2 / \mathbf{w}^H\mathbf{R}_N\mathbf{w}}{\sigma_s^2 / \sigma_n^2}$$

$$AG = \frac{|\mathbf{w}^H\mathbf{d}|^2}{\mathbf{w}^H\mathbf{R}_N\mathbf{w}} \cdot \sigma_n^2$$

对于归一化的噪声协方差（$\text{tr}(\mathbf{R}_N) = P\sigma_n^2$）：

$$AG = \frac{|\mathbf{w}^H\mathbf{d}|^2}{\mathbf{w}^H\mathbf{R}_N\mathbf{w}}$$

**不同噪声场的阵列增益**：

1. **空间白噪声**：$\mathbf{R}_N = \sigma_n^2\mathbf{I}$
   $$AG = WNG$$

2. **扩散噪声场**：$[\mathbf{R}_N]_{ij} = \sigma_n^2 \text{sinc}(kd_{ij})$
   $$AG = DF$$

3. **相干噪声**：$\mathbf{R}_N = \sigma_n^2\mathbf{d}_n\mathbf{d}_n^H$
   $$AG = \frac{|\mathbf{w}^H\mathbf{d}|^2}{|\mathbf{w}^H\mathbf{d}_n|^2}$$

### 4.4 前后比 (Front-to-Back Ratio)

主瓣方向与反方向的功率比：

$$FBR = \frac{|B(\theta_0)|^2}{|B(\theta_0 + \pi)|^2}$$

**dB形式**：
$$FBR_{dB} = 20\log_{10}\frac{|B(\theta_0)|}{|B(\theta_0 + \pi)|}$$

**应用**：
- 评估阵列的前后区分能力
- 对于对称阵列（如线阵），FBR = 0 dB
- 通过非对称设计可以提高FBR

---

## 5. 频率响应

### 5.1 频率依赖性

波束图随频率变化：

$$B(\theta, f) = \mathbf{w}^H(f) \mathbf{d}(\theta, f)$$

**关键参数**：
$$kd = \frac{2\pi f d}{c}$$

**频率效应**：

1. **低频**（$kd \ll 1$）：
   - 主瓣宽
   - 指向性差
   - 阵列增益低

2. **高频**（$kd \gg 1$）：
   - 主瓣窄
   - 指向性强
   - 可能出现栅瓣

### 5.2 栅瓣 (Grating Lobes)

当麦克风间距过大时，在某些方向出现与主瓣幅度相当的响应。

**出现条件**：
$$d > \frac{\lambda}{2}$$

**栅瓣位置**：
$$\sin\theta_g = \sin\theta_0 + n\frac{\lambda}{d}, \quad n = \pm 1, \pm 2, \ldots$$

**避免栅瓣**：
- 保持 $d \leq \lambda_{\min}/2$
- 使用非均匀阵列
- 频率依赖权重

### 5.3 宽带波束形成

对于宽带信号，需要在所有频率上保持一致的波束图。

**频率不变波束形成 (FIB)**：

目标：使波束图在频率范围内保持形状不变。

**方法**：
1. 频率依赖权重
2. 时域FIR滤波器
3. 子带处理

---

## 6. 数值示例

### 6.1 4元线性阵列

**配置**：
- 麦克风数量：$P = 4$
- 间距：$d = 0.05$ m
- 频率：$f = 1000$ Hz, $2000$ Hz, $4000$ Hz
- 目标方向：$\theta_0 = 0°$

**计算**：

波数：
- $f = 1000$ Hz: $k = 18.3$ rad/m, $kd = 0.915$
- $f = 2000$ Hz: $k = 36.6$ rad/m, $kd = 1.83$
- $f = 4000$ Hz: $k = 73.2$ rad/m, $kd = 3.66$

主瓣宽度（第一零点）：
$$\theta_{\text{null}} = \arcsin\left(\frac{\lambda}{Pd}\right) = \arcsin\left(\frac{c}{fPd}\right)$$

- $f = 1000$ Hz: $\theta_{\text{null}} \approx 59°$
- $f = 2000$ Hz: $\theta_{\text{null}} \approx 31°$
- $f = 4000$ Hz: $\theta_{\text{null}} \approx 15°$

**观察**：频率越高，主瓣越窄，指向性越强。

### 6.2 白噪声增益计算

延迟求和波束形成：

$$WNG = P = 4$$
$$WNG_{dB} = 10\log_{10}(4) \approx 6 \text{ dB}$$

### 6.3 指向性因子计算

对于线性阵列的延迟求和：

$$DF \approx \frac{2Pd}{\lambda} = \frac{2Pfd}{c}$$

- $f = 1000$ Hz: $DF \approx 1.17$, $DI \approx 0.7$ dB
- $f = 2000$ Hz: $DF \approx 2.33$, $DI \approx 3.7$ dB
- $f = 4000$ Hz: $DF \approx 4.66$, $DI \approx 6.7$ dB

**观察**：频率越高，指向性因子越大。

---

## 7. 阵列设计准则

### 7.1 间距选择

**准则1：避免空间混叠**
$$d \leq \frac{c}{2f_{\max}}$$

**准则2：最大化孔径**
$$D = (P-1)d$$

**权衡**：
- 小间距：避免混叠，但孔径小
- 大间距：孔径大，但高频混叠

**解决方案**：
- 非均匀间距
- 嵌套阵列
- 频率依赖处理

### 7.2 麦克风数量选择

**考虑因素**：
- 目标阵列增益
- 成本约束
- 计算资源
- 物理尺寸

**经验法则**：
$$P \geq \frac{D}{\lambda_{\min}/2} + 1$$

### 7.3 几何形状选择

| 应用场景 | 推荐几何 | 原因 |
|----------|----------|------|
| 会议系统 | 线性阵列 | 前向拾音 |
| 智能音箱 | 圆形阵列 | 全向覆盖 |
| 声学成像 | 平面阵列 | 2D定位 |
| 助听器 | 2麦克风 | 尺寸限制 |

---

## 8. Python仿真示例

```python
import numpy as np
import matplotlib.pyplot as plt

def beam_pattern_ula(P, d, f, theta0, theta_range):
    """
    计算均匀线性阵列的波束图
    
    参数:
        P: 麦克风数量
        d: 间距 (m)
        f: 频率 (Hz)
        theta0: 目标方向 (度)
        theta_range: 角度范围 (度)
    """
    c = 343  # 声速
    k = 2 * np.pi * f / c
    
    theta = np.deg2rad(theta_range)
    theta0_rad = np.deg2rad(theta0)
    
    # 延迟求和权重
    w = np.exp(1j * k * d * np.arange(P) * np.sin(theta0_rad)) / P
    
    # 计算波束图
    B = np.zeros(len(theta), dtype=complex)
    for i, th in enumerate(theta):
        d_vec = np.exp(-1j * k * d * np.arange(P) * np.sin(th))
        B[i] = np.dot(w.conj(), d_vec)
    
    return theta_range, 20 * np.log10(np.abs(B))

# 示例
P = 4
d = 0.05
frequencies = [1000, 2000, 4000]
theta_range = np.linspace(-90, 90, 361)

plt.figure(figsize=(10, 6))
for f in frequencies:
    theta, B_dB = beam_pattern_ula(P, d, f, 0, theta_range)
    plt.plot(theta, B_dB, label=f'{f} Hz')

plt.xlabel('Angle (degrees)')
plt.ylabel('Beam Pattern (dB)')
plt.title('Beam Pattern of 4-element ULA')
plt.grid(True)
plt.legend()
plt.ylim([-40, 5])
plt.show()
```
