---
tags:
  - 机器学习
  - 深度学习
  - GAN
  - 条件GAN
  - CGAN
created: 2025-01-18
modified: 2025-01-18
difficulty: 中等
related:
  - [[生成器Generator]]
  - [[判别器Discriminator]]
  - [[生成对抗网络GAN/生成式神经网络]]
  - [[生成对抗网络GAN/生成对抗的训练算法]]
---

> [!summary] 核心思想
> 条件生成对抗网络（CGAN）通过引入条件信息（如标签、文本、图像）来指导生成过程，使生成器能够按照特定条件生成目标样本。

# 条件生成对抗网络（Conditional GAN, CGAN）

## 概述

**条件生成对抗网络** 是原始生成对抗网络（GAN）的扩展，通过引入**条件信息**（如标签、文本、图像等）来指导生成过程，使生成器能够按照特定条件生成目标样本（如指定类别的图像、符合文本描述的图像等）。

原始 GAN 的生成过程是无监督的，生成器仅基于噪声向量生成样本，缺乏对生成结果的精细控制。而 CGAN 通过加入条件约束，将生成任务转化为**有条件的概率生成问题**，实现了对生成结果的定向控制。

---

## 1. 模型结构：条件信息的融入

CGAN 在原始 GAN 的生成器（G）和判别器（D）中均引入了**条件输入**，具体结构如下：

### 1.1 生成器（G）

#### 输入

- 噪声向量 $z$（随机采样自潜在空间）
- **条件信息 $y$**（如类别标签、文本嵌入、噪声语音等）

#### 处理方式

- 通常将 $z$ 和 $y$ 拼接（Concatenate）
- 或通过嵌入层（Embedding Layer）融合
- 输入到神经网络中
- 生成符合条件 $y$ 的样本 $\hat{x} = G(z, y)$

### 1.2 判别器（D）

#### 输入

- 真实样本 $x$ 或生成样本 $\hat{x}$
- 条件信息 $y$

#### 输出

- 一个概率值，表示在给定条件 $y$ 下，输入样本是真实样本的概率
- $D(x, y)$ 或 $D(\hat{x}, y)$

---

## 2. 目标函数：条件概率下的对抗优化

CGAN 的目标函数在原始 GAN 的基础上增加了**条件约束**，核心是最大化条件概率下的判别能力，同时让生成器欺骗判别器。

数学表达式为：

$$
\min_G \max_D \mathbb{E}_{x \sim p_{\text{data}}(x)} [\log D(x, y)] + \mathbb{E}_{z \sim p_z(z)} [\log(1 - D(G(z, y), y))]
$$

### 判别器（最大化过程）

- 对于真实样本 $(x, y)$：希望 $D(x, y)$ 尽可能接近 1
- 对于生成样本 $(G(z, y), y)$：希望 $D(G(z, y), y)$ 尽可能接近 0

### 生成器（最小化过程）

- 希望生成的样本 $G(z, y)$ 在给定条件 $y$ 下
- 能被判别器误认为真实样本
- 即让 $D(G(z, y), y)$ 尽可能接近 1

---

## 3. 核心特点：从 "无约束生成" 到 "定向生成"

### 3.1 条件信息的类型

#### 离散条件

- 如类别标签（如 "生成一只猫的图像"）

#### 连续条件

- 如图像属性（如 "生成分辨率为 256×256 的图像"）

#### 结构化条件

- 如文本描述、其他图像
- 例如：图像翻译任务中，输入一张素描图，生成对应的真实图像

### 3.2 与原始 GAN 的区别

| **特性** | **原始 GAN** | **CGAN** |
|---------|-------------|----------|
| **输入** | 仅为噪声 $z$ | 噪声 $z$ + 条件 $y$ |
| **生成结果** | 不可控（任意图像） | 受 $y$ 约束（指定类别） |
| **控制性** | 无 | 强 |

---

## 4. 典型应用场景

### 4.1 带标签的图像生成

- **示例**：给定类别标签 "狗"
- **效果**：生成不同姿态、颜色的狗的图像
- **优势**：优于原始 GAN 的无类别生成

### 4.2 图像翻译（Image-to-Image Translation）

- **如 CycleGAN、pix2pix 等模型**
- 输入源域图像（如卫星地图）作为条件
- 生成目标域图像（如对应位置的真实地图）

### 4.3 文本到图像生成

- 输入文本描述（如 "一只戴着帽子的兔子"）作为条件
- 生成符合描述的图像
- 后续改进模型如 StackGAN、AttnGAN 进一步提升了细节精度

### 4.4 图像编辑与修复

- 给定局部条件（如 "将图像中的红色汽车改为蓝色"）
- 生成修改后的图像

---

## 5. 优缺点分析

### 优点

#### 1. 可控性

- 通过条件信息精确控制生成结果的类别、属性等
- 提升实用性

#### 2. 灵活性

- 条件类型多样化
- 适用于监督、半监督场景（利用部分标签信息）

### 缺点

#### 1. 条件融合难度

- 需合理设计网络结构
- 如拼接、嵌入、注意力机制
- 以有效融合条件信息，避免信息丢失

#### 2. 模式崩溃（Mode Collapse）

- 与原始 GAN 类似
- 生成器可能只生成有限种类的样本以欺骗判别器
- 尤其是条件维度较高时

#### 3. 训练稳定性

- 条件信息可能增加判别器的区分难度
- 需调整训练超参数
- 如梯度惩罚、对抗损失函数改进

---

## 相关链接

- [[生成器Generator]] - 生成器详解
- [[判别器Discriminator]] - 判别器详解
- [[生成对抗网络GAN/生成式神经网络]] - GAN 基本框架
- [[生成对抗网络GAN/生成对抗的训练算法]] - 训练算法详解
- [[生成对抗网络GAN/模型/Cycle GAN]] - CGAN 的变体

## 参考资料

- Mirza, M., & Osindero, S. (2014). Conditional Generative Adversarial Nets. *arXiv preprint arXiv:1411.1784*
- Isola, P., et al. (2017). Image-to-Image Translation with Conditional Adversarial Networks. *CVPR* (pix2pix)
