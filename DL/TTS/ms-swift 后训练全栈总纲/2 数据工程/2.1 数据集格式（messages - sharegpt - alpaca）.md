## 前置知识

- [[2. 数据工程]]

## 0. 定位

> 一句话：ms-swift 内部统一为 **messages 格式**，外部输入兼容 alpaca / sharegpt / openai-style，用 Template 完成最终拼接。

---

## 1. 三种主流格式对照

|格式|来源|典型字段|是否推荐|
|---|---|---|---|
|**sharegpt**|ShareGPT 社区|`{from, value}` 列表|✅ 自动转换|
|原生 query/response|—|`{query, response}`|✅ 单轮简化形式|

## 2. messages 格式（首选）

```jsx
{"messages": [
  {"role": "system",    "content": "You are a helpful assistant."},
  {"role": "user",      "content": "什么是 LoRA?"},
  {"role": "assistant", "content": "LoRA 是一种低秩适配的参数高效微调方法..."}
]}
{"messages": [
  {"role": "user",      "content": "写一首关于秋天的五言绝句"},
  {"role": "assistant", "content": "秋风扫落叶，霜降染层林。月下添新酒，闲庭对弈深。"}
]}
```

## 3. alpaca 格式（兼容）

```jsx
{"instruction": "将下面的句子翻译成英文", "input": "今天天气真好", "output": "The weather is great today."}
{"instruction": "解释什么是 transformer", "input": "", "output": "Transformer 是一种基于 self-attention 的神经网络架构..."}
```

## 4. sharegpt 格式（兼容）

```jsx
{"conversations": [
  {"from": "human", "value": "Hi"},
  {"from": "gpt",   "value": "Hello! How can I help you?"},
  {"from": "human", "value": "讲个笑话"},
  {"from": "gpt",   "value": "..."}
]}
```

## 5. 内部转换流程

![[2.1 数据集格式（messages - sharegpt - alpaca） - 5. 内部转换流程.excalidraw|800]]

## 6. 极简代码：手动加载并 encode

```python
from swift.llm import get_template, get_model_tokenizer, EncodePreprocessor
from datasets import load_dataset

_, tokenizer = get_model_tokenizer('Qwen/Qwen2.5-7B-Instruct', load_model=False)
template = get_template('qwen', tokenizer)

# 任意三种格式皆可
raw = load_dataset('json', data_files='data.jsonl', split='train')

# encode -> input_ids / labels（labels 中 prompt 部分被置 -100）
encoded = raw.map(
    EncodePreprocessor(template=template),
    remove_columns=raw.column_names,
    num_proc=4,
)
print(encoded[0].keys())   # dict_keys(['input_ids', 'labels', 'attention_mask'])
print(tokenizer.decode([t for t in encoded[0]['labels'] if t != -100]))
# 只解码 assistant 回答部分（loss 监督的部分）
```

> [!important]
> 
> **关键观察**：所有格式最终都被规约为 `messages`，由 Template 决定如何插入 system prompt、特殊 token、`<|im_start|>` 等。

## 7. 何时选哪种格式

|场景|推荐格式|
|---|---|
|纯单轮 instruction tuning|alpaca 或 messages|
|自定义任务（embedding/RM/reranker）|专用 schema（见 §2.2）|

---

## 参考文献

1. 自定义数据集：<[https://swift.readthedocs.io/zh-cn/latest/Customization/Custom-dataset.html](https://swift.readthedocs.io/zh-cn/latest/Customization/Custom-dataset.html)>

2. 数据集格式参考：<[https://github.com/modelscope/ms-swift/tree/main/examples/train](https://github.com/modelscope/ms-swift/tree/main/examples/train)>