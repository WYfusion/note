## 前置知识

- [[2. 数据工程]]

- [[ms-swift 后训练全栈总纲]]

## 0. 定位

> 一句话：**只在 assistant 回答处计算 loss**，prompt/system/special token 的 label 全部置 `-100`，由 Template 自动完成。

---

## 1. 为什么需要 loss mask

SFT 的目标是"给定 prompt，预测 response"，因此：

- **prompt** 部分不应被监督（否则模型在学输入的分布）。

- **response** 部分应该被监督（被 loss 拉动）。

- **special token / chat 模板符** 通常也不监督。

PyTorch 中 `CrossEntropyLoss` 的 `ignore_index=-100`，把不需要监督的位置 label 置 -100 即可。

## 2. Qwen template 的 loss mask 示意

```jsx
INPUT (拼接后):
<|im_start|>system
You are a helpful assistant.<|im_end|>
<|im_start|>user
什么是 LoRA?<|im_end|>
<|im_start|>assistant
LoRA 是一种低秩适配...<|im_end|>

LABELS:
[-100] system 部分全部为 -100
[-100] user    部分全部为 -100
[ ✓  ] assistant 回答 token 保留为真实 id
[-100] <|im_end|> 之后的 padding 也是 -100
```

## 3. 极简代码：手动构造 loss mask

```python
import torch
from swift.llm import get_template, get_model_tokenizer

_, tok = get_model_tokenizer('Qwen/Qwen2.5-7B-Instruct', load_model=False)
template = get_template('qwen', tok)

sample = {'messages': [
    {'role': 'system',    'content': 'You are helpful.'},
    {'role': 'user',      'content': '什么是 LoRA?'},
    {'role': 'assistant', 'content': 'LoRA 是一种低秩适配...'},
]}
enc = template.encode(sample)
input_ids = torch.tensor(enc['input_ids'])
labels    = torch.tensor(enc['labels'])

# 验证：被监督的 token 数 = assistant 回答 token 数（含 <|im_end|>）
sup = (labels != -100).sum().item()
print(f'total tokens={len(input_ids)}, supervised tokens={sup}')

# 解码出被监督的部分
print(tok.decode(labels[labels != -100]))
# → "LoRA 是一种低秩适配...<|im_end|>"
```

## 4. 多轮对话：所有 assistant 轮都监督

```python
sample = {'messages': [
    {'role': 'user',      'content': 'Q1'},
    {'role': 'assistant', 'content': 'A1'},   # 监督
    {'role': 'user',      'content': 'Q2'},
    {'role': 'assistant', 'content': 'A2'},   # 监督
]}
# 默认 ms-swift 对所有 assistant 轮都开启 loss
# 如果只想监督最后一轮，可设置 --loss_scale last_round（见 §2.5.6）
```

## 5. 高级：loss_scale（按 token 加权）

ms-swift 提供 `--loss_scale` 控制更细粒度的权重：

|loss_scale|行为|
|---|---|
|`last_round`|仅最后一轮 assistant 监督|
|`react`/`agent_flow` 等|Agent 工具调用专用，对 think/observation 等 tag 加不同权重|

```bash
swift sft \
    --model Qwen/Qwen2.5-7B-Instruct \
    --dataset my-multi-turn-data \
    --loss_scale last_round            # 只监督最后一轮
```

## 6. 易错点

> [!important]
> 
> - **template 选错** → loss mask 完全错位，loss 数值看似正常但实际在学 prompt。
> 
> - **多轮场景下默认监督所有 assistant 轮**：如果你只想监督最后一轮，必须显式 `--loss_scale last_round`。
> 
> - **Agent / 工具调用** 通常需要 `loss_scale=all` 或自定义 react 权重，参见 §9。

## 7. 验证 loss mask 是否正确

```python
# 训练前用一条样本快速验证
enc = template.encode(sample)
for i, (tid, lid) in enumerate(zip(enc['input_ids'], enc['labels'])):
    flag = '✓' if lid != -100 else ' '
    print(f'{flag} {i:4d} {tok.decode([tid]):>10s}')
```

训练前花 30 秒打印一条样本，是排查 loss 异常的最高 ROI 操作。

---

## 参考文献

1. Loss scale 文档：<[https://swift.readthedocs.io/zh-cn/latest/Instruction/Pre-training-and-Fine-tuning.html](https://swift.readthedocs.io/zh-cn/latest/Instruction/Pre-training-and-Fine-tuning.html)>

2. Template 实现：<[https://github.com/modelscope/ms-swift/tree/main/swift/llm/template](https://github.com/modelscope/ms-swift/tree/main/swift/llm/template)>