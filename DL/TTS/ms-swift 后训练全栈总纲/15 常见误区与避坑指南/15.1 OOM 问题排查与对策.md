## 前置知识

- [[15. 常见误区与避坑指南]]

- [[8. 训练加速与显存优化]]

## 0. 定位

> 一句话：OOM = 显存不够。系统化定位顺序——batch / seqlen / grad_ckpt / 优化器 / 量化 / 切片。

---

## 1. 显存四大占用

```jsx
GPU 显存 = 模型权重 + 优化器状态 + 梯度 + 激活

7B fp16 训练（不带优化）:
  权重:        14 GB
  Adam 优化器: 56 GB    (m, v 两个 fp32 状态)
  梯度:        14 GB
  激活:        与 batch_size × seqlen 成正比
  ----------------
  >84 GB → 单卡 80G 不可行
```

## 2. 排查顺序（自上而下）

![[15.1 OOM 问题排查与对策 - 2. 排查顺序（自上而下）.excalidraw|100%]]

## 3. 常用减显存开关

|开关|显存收益|速度代价|per_device_batch_size 1 + grad_accum|~1/N|无|
|---|---|---|---|---|---|
|gradient_checkpointing|激活减 70%|+20-30%|LoRA|梯度+优化器减 99%|+5%|
|QLoRA (NF4)|权重减 75%|+10-15%|FSDP / ZeRO-3|权重 / 优化器分片 1/N|+5-15%|
|CPU offload|+大量|+50-100%|FlashAttention 2/3|激活 ~50%|更快|

## 4. 速查表

```jsx
7B 全参 SFT
  1×A100 80G:  不可（需 84+ GB）
  4×A100 80G + ZeRO-3 + grad_ckpt:  可
  1×A100 80G + LoRA + grad_ckpt:    可（约 30 GB）

70B 全参 SFT
  8×A100 80G + ZeRO-3 + grad_ckpt:  勉强
  16×A100 80G + ZeRO-3:             宽松
  Megatron-LM TP=8 PP=2:            最稳

70B QLoRA
  2×RTX 4090 24G + FSDP-QLoRA:      可（answer.ai 方案）
  1×A100 80G:                       可
```

## 5. 显存监控

```python
import torch, gc

def gpu_status(prefix=''):
    if not torch.cuda.is_available():
        return
    for i in range(torch.cuda.device_count()):
        a = torch.cuda.memory_allocated(i) / 1024 ** 3
        r = torch.cuda.memory_reserved(i) / 1024 ** 3
        m = torch.cuda.max_memory_allocated(i) / 1024 ** 3
        print(f'{prefix} GPU{i}: alloc {a:.1f}G / reserved {r:.1f}G / peak {m:.1f}G')

# 在关键阶段插桩
gpu_status('after model load')
gpu_status('after first forward')
gpu_status('after first backward')
```

## 6. 异常案例

|现象|定位|解决|
|---|---|---|
|第一步前几个 batch 后 OOM|动态 padding 撞上长样本|设 max_length / 排序后分桶|
|评测 / 推理 OOM|vLLM 默认占 90%|降 gpu_memory_utilization 到 0.85|

## 7. 显存调优经验

> [!important]
> 
> - **优先开 FlashAttention 2 + grad_ckpt**：多数情况能直接缓解。
> 
> - **dataloader 排序分桶**：长样本和短样本分桶，避免动态 padding 大幅波动。
> 
> - **max_length 一刀切**：业务可接受范围内尽量短。从 8192 → 4096 显存差距大。
> 
> - **LoRA target_modules**：先用 `q,v`，OOM 时不再加 `all-linear`。

> [!important]
> 
> - 不要把 grad_accum 当显存万能药。它能减小有效 batch 显存，但激活峰值仍由 micro_batch 决定。

---

## 参考文献

1. PyTorch Memory：<[https://pytorch.org/docs/stable/notes/cuda.html#memory-management](https://pytorch.org/docs/stable/notes/cuda.html#memory-management)>

2. DeepSpeed ZeRO：<[https://www.deepspeed.ai/tutorials/zero/](https://www.deepspeed.ai/tutorials/zero/)>