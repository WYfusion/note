## 前置知识

- [[15. 常见误区与避坑指南]]

- [[1. 框架架构与核心对象]]

## 0. 定位

> 一句话：模板（chat template）与 tokenizer 配置不一致，是 ms-swift 训练 / 推理结果不对的常见根因。

---

## 1. 模板的作用

```jsx
messages:
  [{role: user, content: 你好}, {role: assistant, content: 你好！}]
         ↓ chat template 渲染
字符串: <|im_start|>user
你好<|im_end|>
<|im_start|>assistant
你好！<|im_end|>
         ↓ tokenizer.encode
input_ids: [151644, ..., 151645]
labels:    [-100, ..., 151645]   # user 部分 mask
```

## 2. 高发坑

|坑|表现|修复|
|---|---|---|
|ms-swift 模板名与 HF chat_template 不一致|训练 OK 推理跑偏|--template 指定|
|系统 prompt 写在 user role|训练损失正常但推理风格漂|改用 system role|
|多轮对话只学最后一轮|多轮能力差|--loss_scale all|

## 3. 核对模板渲染

```python
from swift.llm import get_model_tokenizer, get_template

_, tok = get_model_tokenizer('Qwen/Qwen2.5-7B-Instruct', load_model=False)
tpl = get_template('qwen', tok)

row = {'messages': [
    {'role': 'system', 'content': '你是助手'},
    {'role': 'user',   'content': '1+1='},
    {'role': 'assistant', 'content': '2'},
]}
encoded = tpl.encode(row)
print('--- 渲染后字符串 ---')
print(tok.decode(encoded['input_ids']))
print('--- labels 中可学习的部分 ---')
label_text_ids = [t for t in encoded['labels'] if t != -100]
print(tok.decode(label_text_ids))
```

如果可学习部分不是 "2<|im_end|>"，就是 mask 出错了。

## 4. 多轮 loss_scale

```bash
# 默认: 只学最后一轮 assistant
swift sft ... --loss_scale default

# 学所有 assistant 轮（推荐多轮场景）
swift sft ... --loss_scale all

# 自定义权重（按 role 加权）
swift sft ... --loss_scale my-scale --loss_scale_config configs/scale.json
```

```json
// configs/scale.json
{
    "assistant": 1.0,
    "tool":      0.0,
    "user":      0.0,
    "system":    0.0
}
```

## 5. 系统 prompt 一致性

```python
# 训练用什么 system，推理就要用什么 system
TRAIN_SYS = '你是公司 X 的客服，回答简洁、礼貌、调用工单工具。'

# 训练数据每条 messages[0] 是 TRAIN_SYS
# 推理时 OpenAI SDK 也要 messages 第一个 role=system content=TRAIN_SYS
# 否则训出来的能力会打折
```

## 6. special tokens 校对

```python
from transformers import AutoTokenizer

base_tok = AutoTokenizer.from_pretrained('Qwen/Qwen2.5-7B-Instruct')
ft_tok = AutoTokenizer.from_pretrained('out/qwen-sft')

for key in ['additional_special_tokens', 'pad_token', 'eos_token', 'bos_token', 'unk_token']:
    a = getattr(base_tok, key, None)
    b = getattr(ft_tok, key, None)
    flag = '✓' if a == b else '⚠️'
    print(f'{flag} {key}: base={a} ft={b}')

# vocab 大小变化也是高危信号
print('vocab:', len(base_tok), '→', len(ft_tok))
```

## 7. 部署侧一致性

```bash
# vLLM / SGLang / LMDeploy 默认会读取 ckpt 里的 tokenizer_config.json 中的 chat_template
# 但有时模板字段不全或被覆盖

# 显式校验
python -c "
from transformers import AutoTokenizer
t = AutoTokenizer.from_pretrained('out/qwen-sft')
print(t.apply_chat_template([
    {'role':'system','content':'X'},
    {'role':'user','content':'hi'}], tokenize=False, add_generation_prompt=True))
"
```

> [!important]
> 
> **SOP**：
> 
> 1. 训练前手动 encode 一条样本，肉眼检查渲染。
> 
> 1. 训练后导出，再用 transformers `apply_chat_template` 校对一次。
> 
> 1. 部署后用 OpenAI SDK 发一条业务样本，对比训练时 system prompt 一致。

> [!important]
> 
> - **不要在训练后修改 special tokens**：会让 ckpt 与 tokenizer 不再匹配。
> 
> - **不要混用模板**：base 与 chat 模板差别巨大，混用 = 全炸。

---

## 参考文献

1. HF chat templates：<[https://huggingface.co/docs/transformers/chat_templating](https://huggingface.co/docs/transformers/chat_templating)>

2. ms-swift Templates：<[https://swift.readthedocs.io/zh-cn/latest/Customization/Custom-template.html](https://swift.readthedocs.io/zh-cn/latest/Customization/Custom-template.html)>