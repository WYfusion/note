## 前置知识

- [[9. Agent 与工具调用训练]]

- [[ms-swift 后训练全栈总纲]]

## 0. 定位

> 一句话：ms-swift 采用 OpenAI 风格的 messages，增加 `tools` 字段定义 schema，`tool` 角色填入 observation。

---

## 1. 基础格式

```jsx
{
  "messages": [
    {"role": "system", "content": "You are a helpful assistant with tools."},
    {"role": "user",   "content": "北京今天天气怎么样?"},
    {"role": "assistant",
     "content": null,
     "tool_calls": [
       {"type": "function",
        "function": {"name": "get_weather", "arguments": "{\"city\":\"北京\"}"}}
     ]},
    {"role": "tool", "name": "get_weather", "content": "晴，25度"},
    {"role": "assistant", "content": "北京今天晴，气温 25度。"}
  ],
  "tools": [
    {"type": "function",
     "function": {
       "name": "get_weather",
       "description": "获取某城市今日天气",
       "parameters": {
         "type": "object",
         "properties": {"city": {"type": "string"}},
         "required": ["city"]
       }
     }}
  ]
}
```

## 2. ReAct 风格转换

部分模型（如 LLaMA Agent 系列）使用 ReAct：

```jsx
Thought: 我需要查天气
Action: get_weather
Action Input: {"city": "北京"}
Observation: 晴，25度
Thought: 我现在有答案了
Final Answer: 北京今天晴，气温 25度。
```

ms-swift 提供转换器：在 Qwen-Agent 格式 ⚡ ReAct。

## 3. Hermes 风格与 Qwen 风格

|风格|工具调用标记|使用模型|**OpenAI / Qwen**|`tool_calls` 字段|Qwen2.5/3|
|---|---|---|---|---|---|
|**Hermes**|`<tool_call>...</tool_call>` 包裹|Hermes / NousResearch|**ReAct**|Thought/Action/Observation 文本|LLaMA Agent|

Hermes 示例：

```jsx
<|im_start|>assistant
<tool_call>
{"name": "get_weather", "arguments": {"city": "北京"}}
</tool_call>
<|im_end|>
```

## 4. 训练示例

```bash
swift sft \
    --model Qwen/Qwen2.5-7B-Instruct \
    --dataset my-agent-data \
    --train_type lora \
    --agent_template hermes \        # hermes / react / qwen / glm4
    --loss_scale react \
    --max_length 8192 \
    --output_dir out/qwen-agent
```

## 5. 多工具 / 多参数

```jsx
{
  "messages": [
    {"role":"user","content":"计算 (3+5)*7 并转为人民币"},
    {"role":"assistant","content":null,
     "tool_calls":[
       {"type":"function","function":{"name":"calc","arguments":"{\"expr\":\"(3+5)*7\"}"}}
     ]},
    {"role":"tool","name":"calc","content":"56"},
    {"role":"assistant","content":null,
     "tool_calls":[
       {"type":"function","function":{"name":"convert_currency",
         "arguments":"{\"amount\":56,\"from\":\"USD\",\"to\":\"CNY\"}"}}
     ]},
    {"role":"tool","name":"convert_currency","content":"406.5"},
    {"role":"assistant","content":"56 美元 = 406.5 人民币。"}
  ],
  "tools": [
    {"type":"function","function":{"name":"calc",
      "parameters":{"type":"object","properties":{"expr":{"type":"string"}}}}},
    {"type":"function","function":{"name":"convert_currency",
      "parameters":{"type":"object","properties":{
        "amount":{"type":"number"},"from":{"type":"string"},"to":{"type":"string"}}}}}
  ]
}
```

## 6. 预处理

```python
from swift.llm import get_template, get_model_tokenizer
import json

model, tokenizer = get_model_tokenizer('Qwen/Qwen2.5-7B-Instruct', load_model=False)
template = get_template('qwen2_5', tokenizer)
template.set_default_mode('train')   # 启用 loss_mask

with open('agent_sample.json') as f:
    sample = json.load(f)

enc = template.encode(sample)
print('input_ids 长度:', len(enc['input_ids']))
print('有效 label 数:', sum(1 for x in enc['labels'] if x != -100))
```

## 7. 踩坑提示

> [!important]
> 
> - **arguments 必须是 JSON 字符串**，不是 object。错了会导致训练时序列化出问题。
> 
> - **agent_template 与推理一致**：训练用 hermes，推理也要 hermes，否则模型不会调用工具。
> 
> - **工具过多会被截断**：定义超过 30 个工具时 prompt 占多，建议分组或动态筛选。

---

## 参考文献

1. OpenAI Function Calling：<[https://platform.openai.com/docs/guides/function-calling](https://platform.openai.com/docs/guides/function-calling)>

2. Qwen-Agent：<[https://github.com/QwenLM/Qwen-Agent](https://github.com/QwenLM/Qwen-Agent)>

3. ms-swift Agent：<[https://swift.readthedocs.io/zh-cn/latest/Instruction/Agent-support.html](https://swift.readthedocs.io/zh-cn/latest/Instruction/Agent-support.html)>