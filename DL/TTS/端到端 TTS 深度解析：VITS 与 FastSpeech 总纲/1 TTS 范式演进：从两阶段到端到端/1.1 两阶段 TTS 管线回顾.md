## 定位

> Tacotron2 + HiFi-GAN 的标准管线、各模块职责、固有局限

---

## 1. 两阶段 TTS 标准管线

```mermaid
flowchart LR
    TEXT["文本输入"] --> G2P["G2P
文本→音素"]
    G2P --> AM["声学模型
Tacotron2"]
    AM --> MEL["Mel 频谱图
(80-dim, ~12.5ms/帧)"]
    MEL --> VOC["声码器
HiFi-GAN"]
    VOC --> WAV["波形输出
(22.05kHz)"]
    style AM fill:\#3498DB,color:\#fff
    style VOC fill:\#E74C3C,color:\#fff
```

---

## 2. Tacotron2 核心架构

```mermaid
flowchart TD
    CHARS["字符/音素"] --> ENC["Encoder
3层 Conv + BiLSTM"]
    ENC --> ATTN["Location-Sensitive
Attention"]
    ATTN --> DEC["Decoder
2层 LSTM"]
    DEC --> MEL_OUT["Mel 帧
(逐帧自回归)"]
    MEL_OUT -->|"前一帧作为下一步输入"| DEC
    style ATTN fill:\#E74C3C,color:\#fff
```

**自回归的本质**：每生成一帧 Mel，都要等前一帧完成。一段 5 秒语音 ≈ 400 帧，意味着 400 步串行推理。

> [!important]
> 
> **思辨：Tacotron2 的 Attention 为什么不可靠？** Location-Sensitive Attention 是「软对齐」——每一步对所有音素都有权重。这意味着模型**可能跳过**某些音素（导致漏字）或**反复关注**同一音素（导致重复）。尤其在长句、罕见词、数字序列上问题严重。FastSpeech 和 VITS 分别用 Duration Predictor 和 MAS 解决了这个问题，本质上都是用**硬对齐**替代**软对齐**。