# Qwen3-TTS 训练数据集与方法原理
本页面深入解析 Qwen3-TTS 的完整训练流程，包括数据集的具体形式与构成、数据分层策略、以及预训练和后训练中各阶段方法的详细原理。

---
## 1. 训练数据总览

### 1.1 数据规模与来源
📦
**总数据量**：超过 **500 万小时**的多语言语音数据
**语种覆盖**：10+ 种语言（中文、英文、意大利语、法语、韩语、俄语、德语、西班牙语、日语、阿拉伯语等）
### 1.2 数据的具体形式
训练数据以**文本-语音对**（text-speech pairs）的形式组织，每条样本包含：

| **字段** | **内容** | **说明** |
| --- | --- | --- |
| 音频波形 | 原始语音信号 | 采样率通常为 16kHz 或更高 |
| 文本转录 | 对应的文字内容 | 可通过人工标注或 ASR 系统自动生成 |
| 说话人标签 | 说话人 ID / Embedding | 用于训练说话人编码器和身份控制 |
| 语言标签 | 语种信息 | 支持多语言路由和训练 |
| 语音 Token | Tokenizer 编码后的离散序列 | 25Hz 单码本或 12Hz 16层 RVQ |

**ChatML 格式标准化**：
所有训练样本被统一封装为 ChatML 格式的对话序列，使 TTS 任务与 LLM 的训练范式一致：
```bash
<|im_start|>system
[系统提示：说话人信息、控制指令等]
<|im_end|>
<|im_start|>user
[输入文本内容]
<|im_end|>
<|im_start|>assistant
[语音 token 序列]
<|im_end|>
```
这种格式的优势：
- 直接复用 Qwen3 LM 的预训练知识和指令跟随能力
- 统一支持基础 TTS、语音克隆、Voice Design 等多种任务
- 不同任务仅通过 system prompt 的变化进行切换

### 1.3 数据来源类型
根据论文描述，数据可能包括以下几类来源：
- **有声读物 / 播客数据**：长时、连续、朗读风格，适合预训练基础能力
- **对话语音数据**：多轮对话录音，提供口语化韵律
- **多语言语料库**：覆盖多语种的标注或弱标注语音数据
- **高质量录音室数据**：专业录制的播音员、配音演员语音，用于高质量阶段
- **合成与增强数据**：可能包含经过数据增强的样本

---

## 2. 预训练三阶段详解

### 2.1 Stage 1：通用阶段（General Stage）


1️⃣
**目标**：建立多语言文本到语音的基础映射能力
**数据策略**：
- 使用**全部 500 万+ 小时**数据
- 不对数据质量进行严格筛选，重点在于**规模和多样性**
- 最大 token 长度限制为 **8,192**

**训练目标与原理**：
- 标准的**自回归语言建模（Autoregressive Language Modeling）**
- 损失函数：**交叉熵损失（Cross-Entropy Loss）**
- 对于每个时间步 $t$，模型根据前序 token $x_{<t}$ 预测当前语音 token $x_t$
- 数学形式：$mathcal{L} = -sum_{t=1}^{T} log P(x_t | x_{<t}, text_{text}, text_{speaker})$

**这一阶段学到了什么**：
- 文本到语音的基本**单调对齐**（文本从左到右依次生成对应语音）
- 多语言的发音规则和韵律模式
- 基础的说话人身份建模
- 但由于数据中包含大量噪声样本（录音环境差、转录不准确等），模型可能会产生**幻觉**（如跳词、重复、发音错误等）

### 2.2 Stage 2：高质量阶段（High-Quality Stage）

2️⃣
**目标**：提升生成质量，缓解 Stage 1 中噪声数据导致的幻觉问题
**数据分层方法（核心）**：
这是整个训练流程中至关重要的一环。论文提到通过「专用 pipeline」对数据质量进行分层。根据 TTS 领域的通用实践和论文线索，分层策略可能包含以下维度：
**维度 1：音频质量评估**
- **信噪比（SNR）筛选**：过滤背景噪声过大的样本
- **DNSMOS / PESQ 自动评分**：使用预训练的音频质量评估模型为每条样本打分
- **录音环境分类**：区分录音棚级、安静室内、嘈杂环境等

**维度 2：转录准确性评估**
- **ASR 交叉验证**：用 ASR 模型对音频进行转录，计算转录结果与标注文本之间的 WER/CER
- 保留 WER 低于阈值的样本（转录准确的高质量对齐数据）
- 过滤对齐不良的样本（可能存在标注错误或截断）

**维度 3：说话人一致性**
- **说话人验证**：确保同一说话人 ID 下的所有录音确实来自同一人
- **Speaker Embedding 聚类**：检测并剔除被错误标注的样本

**维度 4：韵律与表现力评估**
- **韵律平滑度**：检测不自然的停顿、断句
- **语速一致性**：过滤语速异常的样本
- **情绪标注验证**：确保情绪标签与实际语音匹配

**分层结果**：
- 数据被分为多个质量层级（如：高/中/低 质量）
- Stage 2 仅使用**高质量层**数据进行 CPT（Continual Pre-Training）
- 数据量显著减少（从 500万+ 小时缩减到精选子集），但每条样本的质量远高于 Stage 1

**CPT（Continual Pre-Training）原理**：
- 在 Stage 1 的 checkpoint 基础上继续训练
- 使用与 Stage 1 相同的自回归建模目标
- 但通过高质量数据**纠正**模型在 Stage 1 中学到的不良模式
- 可能使用较小的学习率，避免灾难性遗忘（catastrophic forgetting）

**效果**：
- 显著减少合成语音中的幻觉现象
- 提升发音准确性和自然度
- 增强韵律的合理性和流畅性

### 2.3 Stage 3：长上下文阶段（Long-Context Stage）
3️⃣
**目标**：扩展模型处理长文本和长语音的能力
**关键变化**：
- 最大 token 长度从 8,192 扩展到 **32,768**（4 倍）
- 对训练数据中的**长语音样本进行上采样**（oversampling）

**数据上采样策略**：
- 在原始数据分布中，长语音样本（>30秒、>1分钟、>5分钟等）通常占比很小
- 通过增加这些长样本的采样频率/权重，使模型在训练中接触更多的长上下文场景
- 可能还包括将多条短语音拼接为长序列的数据增强策略

**RoPE 位置编码扩展**：
- Qwen3 LM 使用 RoPE（Rotary Position Embedding）
- 扩展到 32K 上下文长度可能涉及：
    - **RoPE 基频调整（base frequency scaling）**：修改 RoPE 的基频参数以支持更长位置
    - **渐进式长度扩展**：逐步增加训练样本的最大长度

**解决的问题**：
- 在 Stage 1/2 中，模型主要在 ≤8K token 的样本上训练，处理更长输入时可能出现：
    - 韵律断裂（长距离的语调和节奏不连贯）
    - 内容遗漏或重复
    - 注意力分散导致的质量下降
- Stage 3 通过长上下文训练克服这些问题

**效果**：支持连续合成 **10+ 分钟**的流畅语音（对应约 15,000-20,000 个语音 token）

---

## 3. 后训练三阶段详解

### 3.1 Stage 1：DPO（Direct Preference Optimization）
🎯
**目标**：将模型输出对齐人类偏好，提升主观感知质量
**偏好数据构建**：

1. 对于同一段输入文本，使用模型（或多个模型变体）生成**多条候选语音**
2. 由人类评审员进行**成对比较（pairwise comparison）**：
    - 比较维度：自然度、流畅性、发音准确性、韵律合理性、音色一致性等
    - 对于每对候选，标注哪一条更好 → 形成**偏好对（preference pair）**
3. 偏好数据格式：$(x, y_w, y_l)$，其中 $x$ 为输入，$y_w$ 为偏好样本（winner），$y_l$ 为非偏好样本（loser）
4. 构建覆盖**多语言**的偏好数据集

**DPO 原理**：

DPO（Rafailov et al., 2023）是 RLHF（Reinforcement Learning from Human Feedback）的简化替代方案：

- 传统 RLHF 流程：训练奖励模型 → 用 PPO 优化策略 → 复杂且不稳定
- DPO 核心洞察：**奖励模型可以被解析地表示为策略模型本身的函数**，因此可以跳过显式的奖励模型训练
- 损失函数：

$\mathcal{L}_{DPO} = -\mathbb{E}\left[\log \sigma \left( \beta \log \frac{\pi_\theta(y_w|x)}{\pi_{ref}(y_w|x)} - \beta \log \frac{\pi_\theta(y_l|x)}{\pi_{ref}(y_l|x)} \right)\right]$

其中：

- $\pi_\theta$ 为当前策略模型（正在训练的模型）
- $\pi_{ref}$ 为参考模型（Stage 3 的 checkpoint，固定不变）
- $\beta$ 为温度参数，控制偏离参考模型的程度
- $\sigma$ 为 sigmoid 函数

**直觉理解**：这个损失函数同时做两件事：

1. 增加模型生成偏好样本 $y_w$ 的概率
2. 降低模型生成非偏好样本 $y_l$ 的概率
3. 同时通过 KL 散度约束不让模型偏离参考模型太远（防止过拟合偏好数据导致多样性崩溃）

**在 TTS 场景中的特殊考量**：

- 语音偏好比文本偏好更**主观**，评审者之间一致性可能较低
- 需要控制比较维度的粒度（整体偏好 vs 维度偏好）
- 多语言场景下，不同语言的评审标准可能不同

### 3.2 Stage 2：GSPO + Rule-based Rewards

📏
**目标**：在 DPO 基础上，使用可量化的规则奖励全面增强模型能力
**Rule-based Rewards（基于规则的奖励）**：
与 DPO 依赖人类标注不同，这一阶段使用**自动化、可计算的奖励信号**：

| **奖励维度** | **计算方法** | **优化目标** |
| --- | --- | --- |
| 内容准确性 | ASR 转录 → 计算 WER/CER | 降低幻觉、跳词、重复 |
| 说话人一致性 | Speaker Embedding 余弦相似度 | 提升语音克隆中的音色保持 |
| 音频质量 | UTMOS / DNSMOS 自动评分 | 提升整体音质和自然度 |
| 韵律合理性 | F0 轨迹分析、停顿检测 | 优化语调和节奏 |
| 属性匹配 | 属性分类器准确率 | 提升可控性指令跟随 |

**GSPO 原理**：
GSPO（Group Sampling Policy Optimization）是一种结合了**分组采样**和**策略优化**的方法，可以看作是 GRPO（Group Relative Policy Optimization）家族的变体：

1. 对每条输入 $x$，从当前策略采样 **一组**候选输出 $\{y_1, y_2, ..., y_G\}$
2. 用 Rule-based Rewards 为每条候选计算奖励分数 $\{r_1, r_2, ..., r_G\}$
3. 在组内进行**相对排序**，识别优劣样本
4. 类似 DPO 的方式更新策略：增加高奖励样本的概率，降低低奖励样本的概率

**相比 DPO 的优势**：
- 不依赖人类标注，可以**大规模自动化**
- 奖励信号更**精确和多维**（多个 rule 可以分别衡量不同方面）
- 可以在训练过程中**动态生成偏好对**，避免 DPO 中固定偏好数据的分布偏移问题
- 多个奖励维度可以加权组合，灵活调整优化方向

**效果**：全面提升模型在各维度上的能力和稳定性

### 3.3 Stage 3：Speaker Fine-tuning（说话人微调）

🎙️
**目标**：使模型能够采用特定的固定语音身份，提升自然度和可控性
**微调策略**：
- 在 Base 模型（经过 Stage 1-2 后训练的模型）上进行**轻量级微调**
- 针对特定说话人的高质量数据进行精调
- 可能使用的技术：
    - **LoRA（Low-Rank Adaptation）**：仅训练少量低秩矩阵，保持主体参数不变
    - **全参数微调（小学习率）**：使用极小的学习率对全部参数进行微调
    - **Adapter 层**：插入轻量级 adapter 模块

**数据形式**：
- 特定说话人的高质量录音（可能是录音棚级别）
- 每个目标说话人可能需要几十分钟到几小时的数据
- 数据涵盖不同的文本内容、语速、情绪等

**产出模型**：
- 这一阶段产出的是**CustomVoice** 变体（如 Qwen3-TTS-12Hz-1.7B-CustomVoice）
- 每个 CustomVoice 模型绑定一个特定的说话人身份
- 相比 Base 模型的零样本克隆，CustomVoice 具有更高的音色稳定性和自然度

---

## 4. 特殊训练技巧

### 4.1 概率激活 Thinking Pattern
💭
**用于 Voice Design 和细粒度控制任务**
**问题**：当用户给出复杂的自然语言描述（如"一个 30 岁左右的男性，声音低沉而温暖，语速略慢，带有轻微的播音员腔调"），模型需要理解并执行多个属性控制。
**方法**：
- 在训练过程中，以一定概率（非每次都触发）激活 **Chain-of-Thought（思维链）**模式
- 模型先用文本推理分析用户描述中的各个属性要求
- 然后基于推理结果生成语音
- "概率激活"而非"总是激活"的原因：
    - 避免模型过度依赖思维链，对简单指令也能快速响应
    - 平衡推理质量和推理延迟

### 4.2 MTP 模块训练（12Hz 变体特有）
- MTP 模块与 LM 骨干**联合训练**
- 训练时，第 0 层使用 Teacher Forcing（GT token），MTP 模块预测第 1-15 层
- 各层的预测损失可能使用不同权重（语义层权重更高）
- 推理时，第 0 层使用 LM 的自回归预测，MTP 使用第 0 层的预测结果

---

## 5. 训练流程总结


🗺️
**完整训练路径**
**预训练阶段**
1. **General Stage**：500万+ 小时全量数据 → 建立基础文本-语音映射 → 最大长度 8K
2. **High-Quality Stage**：质量分层后的高质量子集 → CPT 纠正幻觉 → 提升质量
3. **Long-Context Stage**：长样本上采样 → 最大长度扩展至 32K → 支持 10+ 分钟合成
**后训练阶段**
4. **DPO**：人类偏好对 → 对齐主观感知质量
5. **GSPO + Rules**：自动化多维奖励 → 全面增强各项能力
6. **Speaker SFT**：特定说话人数据 → 轻量微调 → CustomVoice 模型

---

## 6. 关键数据集参考

论文中用于**评测**（非训练）的数据集包括：

| **数据集** | **用途** | **说明** |
| --- | --- | --- |
| LibriSpeech test-clean | Tokenizer 重建质量评估 | 英文朗读语音，高质量、干净环境 |
| CommonVoice (C.V.) | ASR 语义保留评估 | 多语言众包语音数据集 |
| Fleurs | ASR 语义保留评估 | Google 多语言语音理解评估集 |
| Seed-TTS Benchmark | 零样本语音克隆评估 | ByteDance 提出的 TTS 评估基准 |
| InstructTTSEval | 可控语音生成评估 | 评估指令跟随和属性控制能力 |
| 自建长语音测试集 | 长语音（>10分钟）稳定性 | 中英文长语音评测 |
| 自建多语言测试集 | 10语种生成质量 | 覆盖 10 种语言的评测样本 |
| 自建跨语言测试集 | 跨语言克隆评估 | 如中→英、英→中、中→韩等翻译对 |