## 权衡

固定比特率（如 6kbps）下有两种配置：

- **深而小**：$N_q=8, N=1024$（8×10bit）

- **浅而大**：$N_q=4, N=2^{20}$（4×20bit）

## SoundStream 的选择

$N_q=8, N=1024$ 显著优于其他配置。

> [!important]
> 
> **更多级比更大码本更有效。** 直觉：RVQ 的级联结构提供了**层级化的表示**——第 1 层粗糙、后续层细化。这比在一个巨大的平坦码本中搜索更高效，因为语音信号本身就有层级结构（基频→谐波→纹理）。