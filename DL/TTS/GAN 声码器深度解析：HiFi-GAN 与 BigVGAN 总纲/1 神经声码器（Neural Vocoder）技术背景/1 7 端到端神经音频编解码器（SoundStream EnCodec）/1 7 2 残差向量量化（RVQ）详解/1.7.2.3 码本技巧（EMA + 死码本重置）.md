## EMA 更新

$$e_k \leftarrow \gamma \cdot e_k + (1-\gamma) \cdot \bar{z}_k$$

$\bar{z}_k$ 是被分配到码本 $k$ 的编码器输出的均值。$\gamma=0.99$ 或 0.999。

## 死码本重置

当某个码本向量的使用率低于阈值（如 1/N）时，随机重置为当前 batch 的编码器输出。

> [!important]
> 
> **码本利用率是实际难题。** RVQ 后面几层的码本利用率往往很低（残差越来越小，分布集中）。死码本重置是经验性方案，缺乏理论保证，但实践中有效。