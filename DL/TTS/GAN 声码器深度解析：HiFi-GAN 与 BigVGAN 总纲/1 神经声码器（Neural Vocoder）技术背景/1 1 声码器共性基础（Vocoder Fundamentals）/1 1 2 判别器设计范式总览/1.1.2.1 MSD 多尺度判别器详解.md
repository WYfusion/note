## 原理

对波形应用 ×1, ×2, ×4 平均池化后分别送入子判别器，捕获不同时间尺度的结构。

## 架构

- 3 个子判别器，共享架构但**不共享权重**

- 每个子判别器：4 层 1D Conv + LeakyReLU + 1 层输出

- 下采样池化：`nn.AvgPool1d(4, stride=2, padding=2)`

> [!important]
> 
> **局限**：下采样会平滑高频细节，MSD 对高频伪影不敏感。这是 HiFi-GAN 引入 MPD 的原因。BigVGAN 用 MRD（频域判别）替换 MSD 进一步改善。