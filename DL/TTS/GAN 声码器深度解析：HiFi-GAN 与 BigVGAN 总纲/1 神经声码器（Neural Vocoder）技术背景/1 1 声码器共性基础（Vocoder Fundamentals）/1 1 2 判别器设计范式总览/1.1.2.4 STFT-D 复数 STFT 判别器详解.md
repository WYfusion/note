## 原理

SoundStream 使用的判别器。输入是 STFT 的**实部和虚部**（而非幅度），保留了相位信息。

## 与 MRD 的区别

- MRD 输入：STFT **幅度谱**（丢弃相位）

- STFT-D 输入：STFT **实部 + 虚部**（保留相位）

## 结构

在复数 STFT 上应用 2D 卷积，时间维和频率维同时处理。

> [!important]
> 
> **保留相位的价值**：STFT-D 可以检测相位不连续性（如帧间相位跳变），这是纯幅度判别器无法发现的伪影。这对 Codec 场景尤其重要——量化误差往往表现为相位噪声。