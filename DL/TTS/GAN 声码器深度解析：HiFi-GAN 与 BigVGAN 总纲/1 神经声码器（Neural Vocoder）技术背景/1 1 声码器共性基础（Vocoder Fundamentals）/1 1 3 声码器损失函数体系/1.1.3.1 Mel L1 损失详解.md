## 定义

$$\mathcal{L}_{\text{mel}} = \| \text{Mel}(x) - \text{Mel}(G(z)) \|_1$$

在 Mel 域计算 L1 距离，而非波形域。

## 为什么在 Mel 域

- Mel 域对应人耳感知，比波形域 L1 更有感知意义

- 80 维 vs 22000 维，梯度更稳定

- HiFi-GAN 权重 $\lambda_{\text{mel}}=45$，是最重要的稳定器

> [!important]
> 
> **Mel L1 的局限**：它只约束频谱包络，不约束相位和高频细节。因此需要对抗损失和特征匹配损失补充。