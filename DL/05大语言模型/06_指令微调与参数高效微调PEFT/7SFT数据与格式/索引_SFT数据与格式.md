# SFT数据与格式

本章节详细介绍了监督微调（SFT）中的数据构建、格式规范及模板化处理，特别针对语音大模型（Audio LLM）的多模态数据特性进行了补充。

## 目录

### [01_指令数据设计_单轮多轮_角色.md](01_指令数据设计_单轮多轮_角色.md)
- **核心概念**：指令（Instruction）、输入（Input）、输出（Output）的三元组结构。
- **数据类型**：
  - **单轮指令**：QA问答、任务执行。
  - **多轮对话**：上下文依赖、指代消解。
- **语音特有**：
  - **ASR数据**：音频转录作为Output。
  - **TTS数据**：文本作为Input，音频参数/Code作为Output。
  - **语音问答**：`<Audio>`占位符的使用。

### [02_模板化_ChatML_系统提示词.md](02_模板化_ChatML_系统提示词.md)
- **Prompt Template**：将结构化数据转换为模型输入的字符串格式。
- **ChatML格式**：OpenAI/Qwen采用的 `<|im_start|>` 角色标记格式。
- **System Prompt**：动态设定模型行为（如“你是一个语音助手”）。
- **特殊Token处理**：
  - 语音Token（Audio Token）在模板中的位置。
  - 跨模态对齐时的特殊标记（Special Tokens）。


### 灾难性遗忘

^31fc71

针对单个任务进行微调可能会导致一种称为**灾难性遗忘**的问题。这是因为完全微调会改变原始 LLM 的权重。虽然这能显著提升我们正在微调的任务的性能，但却会降低其他任务的性能。

例如，一个针对情感分析进行微调的模型可能在该任务上表现非常出色，但即使在微调之前在命名实体识别方面表现良好，但在命名实体识别方面也可能失败。

#### 避免灾难性遗忘
首先，我们必须弄清楚我们的模型是否真的会受到这个问题的影响。例如，如果我们只要求模型在我们正在微调的单个任务上具有可靠的性能，那么我们就无需担心灾难性遗忘。
但是，如果我们希望模型保持其多任务性能，我们可以同时对多个任务进行微调。这通常需要跨多个任务的 5 万到 10 万个样本。