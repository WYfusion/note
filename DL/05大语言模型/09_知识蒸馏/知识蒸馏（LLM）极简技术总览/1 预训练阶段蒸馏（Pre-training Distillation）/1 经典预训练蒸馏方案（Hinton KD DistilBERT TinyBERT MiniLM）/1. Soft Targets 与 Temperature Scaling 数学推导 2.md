本页面从数学角度严谨推导 Hinton KD 中 soft targets 与 temperature scaling 的原理。

---

## 1. 标准 Softmax 与温度缩放

给定 logits 向量 $z in mathbb{R}^V$（$V$ 为词表大小），引入温度 $T > 0$ 后的 tempered softmax：

$$q_i^{(T)} = \frac{\exp(z_i / T)}{\sum_{j=1}^{V} \exp(z_j / T)}$$

**温度效果**：$T=1$ 标准 softmax；$T to 0^+$ 趋向 one-hot；$T to infty$ 趋向均匀分布。

> [!important] 直觉

> 高温使分布"变软"，暴露出 teacher 对各类别相对关系的细微判断（"暗知识"）。

---

## 2. KL 散度蒸馏损失

$$L_{KD} = KL(q_T^{(T)} \| q_S^{(T)}) = -\sum_{i} q_{T,i}^{(T)} \log q_{S,i}^{(T)} + \text{const}$$

优化等价于最小化交叉熵 $H(q_T^{(T)}, q_S^{(T)})$。

---

## 3. $T^2$ 缩放因子推导

对 student logits $z_S$ 求梯度：

$$\frac{\partial L_{KD}}{\partial z_{S,i}} = \frac{1}{T}(q_{S,i}^{(T)} - q_{T,i}^{(T)})$$

梯度含 $1/T$ 因子，KL 本身在高温下也缩小约 $1/T$，合计约 $1/T^2$。乘以 $T^2$ 恢复梯度原始量级：

$$L_{KD}^{scaled} = T^2 \cdot KL(q_T^{(T)} \| q_S^{(T)})$$

---

## 4. 完整蒸馏损失

$$L_{total} = \alpha \cdot L_{CE}(y, p_S) + (1 - \alpha) \cdot T^2 \cdot KL(q_T^{(T)} \| q_S^{(T)})$$

- $alpha in [0.1, 0.5]$：平衡系数

- $T in [2, 20]$：温度，常用 $T=4$

---

## 5. 高温近似分析

当 $T \gg \max(|z_i|)$ 时：

$$KL(q_T^{(T)} \| q_S^{(T)}) \approx \frac{1}{2VT^2} \|z_T - z_S\|_2^2$$

> [!important] 关键结论

> 高温极限下，KL 蒸馏退化为 logits 的 **MSE 损失**。

---

## 6. Python 实现

```Python
import torch
import torch.nn.functional as F

def hinton_distillation_loss(
    student_logits: torch.Tensor,  # (B, V)
    teacher_logits: torch.Tensor,  # (B, V)
    labels: torch.Tensor,          # (B,)
    temperature: float = 4.0,
    alpha: float = 0.3,
) -> torch.Tensor:
    """Hinton KD loss: hard label CE + soft label KL."""
    # Hard label loss (T=1)
    loss_ce = F.cross_entropy(student_logits, labels)

    # Soft label loss (tempered)
    student_soft = F.log_softmax(student_logits / temperature, dim=-1)
    teacher_soft = F.softmax(teacher_logits / temperature, dim=-1)
    loss_kd = F.kl_div(student_soft, teacher_soft, reduction="batchmean")

    # Combined loss with T^2 scaling
    loss = alpha * loss_ce + (1 - alpha) * (temperature ** 2) * loss_kd
    return loss
```

---

## 7. 超参数选择指南

|超参数|推荐范围|选择逻辑|
|---|---|---|
|$T$|2~10|teacher 越强、student 越弱 → T 越大|
|$\alpha$|0.1~0.5|数据充足 → 偏低（更多依赖 teacher）|
|teacher size|2x~10x student|gap 过大效果反降|